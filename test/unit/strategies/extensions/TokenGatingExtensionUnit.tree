TokenGatingExtension::_checkOnlyWithToken
├── When token address is zero
│   └── It should revert
├── When actor address is zero
│   └── It should revert
└── When actor balance is less than amount
    └── It should revert